# name: "Create env file"

# on:
#   push:
#     branches:
#       - main

#   # schedule:
#   #   # * is a special character in YAML so you have to quote this string
#   #   - cron: "*/30 * * * *"

# permissions:
#   contents: write

# jobs:
#   create_env_file:
#     runs-on: ubuntu-latest
#     # steps:
#     #   - name: Create .env file
#     #     uses: ozaytsev86/create-env-file@v1
#     #     with:
#     #       ENV_MONGODB_DATA_API_KEY: ${{ secrets.MONGODB_DATA_API_KEY }}

#     steps:
#       - name: Make envfile
#         uses: SpicyPizza/create-envfile@v1.3
#         with:
#           # envkey_DEBUG: false
#           # envkey_SOME_API_KEY: "123456abcdef"
#           envkey_MONGODB_DATA_API_KEY: ${{ secrets.MONGODB_DATA_API_KEY }}
#           # some_other_variable: foobar
#           directory: assets/
#           file_name: .env
#           fail_on_empty: false

name: Create env file

on: [push]

env: # env available for all jobs all steps
  PRODUCTION: true
  ENV_KEY_PROJECT_NAME: dot-env

jobs:
  create-env-file:
    name: Create env file
    runs-on: ubuntu-latest
    env: # env available for all steps of jobs
      ENV_KEY_DEBUG: true
      ENV_KEY_USERNAME: root

    steps:
      - uses: actions/checkout@v2

      - uses: iamsauravsharma/create-dotenv@v1.2.2
        with:
          env-prefix: "ENVKEY_" # Optional (default: '')
          # file-name: 'development.env' # Optional (default : '.env')
          directory: "assets/" # Optional (default: '.')
        env: # env available for only this steps
          IS_SERVER: false
          # ENV_KEY_USERNAME: admin
          ENVKEY_MONGODB_DATA_API_KEY: ${{ secrets.MONGODB_DATA_API_KEY }}
          # ENV_KEY_SECRET_KEY: secret123
